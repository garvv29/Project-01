### Session 1
* Date: 23-06-2026 (2.00PM-4.00PM)
* Duration: 2 hours
* Goal: Dynamic Array Implementation (Methods)
* Problem Encountered:
1. Every constructor had repetitive code of setting size,cap and data to 0 and nullptr so i made a method called clear to use it in constructors to avoid repetitive code but this method caused memory leaks when checked through valgrind. 
2. another bug i found was clear wasnt working because i set size to 0 first and then deleted the data so the destroyelements loop never ran
3. in the [] operator overloading i found out that modifying the data at index 100 in a array of size 5 was possible and didnt throw an error but the valgrind check found it .
```cpp
==36779== Invalid write of size 4
==36779==    at 0x400277C: main (in /mnt/c/users/garvc/desktop/cq/project-01/tests/test)
==36779==  Address 0x4e9dee0 is 304 bytes inside an unallocated block of size 4,117,040 in arena "client"
==36779== HEAP SUMMARY:
==36779==     in use at exit: 0 bytes in 0 blocks
==36779==   total heap usage: 17 allocs, 17 frees, 76,028 bytes allocated
==36779== 
==36779== All heap blocks were freed -- no leaks are possible
```
4. while i was making my set method it gave an error while i tested it 
```cppp
test.cpp: In function ‘int main()’:
test.cpp:39:13: error: cannot bind non-const lvalue reference of type ‘int&’ to an rvalue of type ‘int’
   39 |     c.set(1,10);
      |             ^~
In file included from test.cpp:1:
/mnt/c/Users/garvc/Desktop/cq/Project-01/src/DynamicArray:147:27: note: initializing argument 2 of ‘void DynamicArray<T>::set(int, T&) const [with T = int]’
  147 |     void set(int index,T& value)const{
      |                        ~~~^~~~~
```
5. found bugs in get and set method where i checking out of bounds incorrectly (index<size) which caused an array of size 5 to access index 5.
* What I Tried:
1. I made the method clear used for just clearing data not to set size,cap and data to nullptr instead i used a init method to set size cap and data to nullptr or explicitly used colons to initialize size,cap and data to 0 and nullptr in constructors.
2. destroyed elements first then set size to 0 in clear method.
3. i researched and found out that cpp is made for being fast so it has a seperate at() function to do bounds checking and [] operator overloading is just for accessing the data without bounds checking.altho python and java does it and gives an out of bounds error
4. for that when i searched for it i found out that the set method without const would need a Lvalue reference which is a variable that has a memory address and can be modified but the value 10 is a Rvalue which is a temporary value that doesnt have a memory address and cant be modified so i changed the set method to take a const reference instead of a nonconst reference.
5. fixed the get and set method to check for out of bounds correctly (index>=size || index<0)

* Outcome: tested all the constructors , clear, append ,[] operator overloading ,get ,set,insert using some of my testcases normally and using valgrind


also had to change my resize logic that firstly checked if the new cap is stricly greater than old cap or not but that would've been a problem for using resize to shrink the array according to my shrinking logic so for that i changed the condition to check if the new cap is greater than or equal to size_ so that it can shrink the array if needed. and my shrinking logic will only shrink when i have size = cap/4 so and in that case the new cap will be cap/2 which would obv be greater than size 

