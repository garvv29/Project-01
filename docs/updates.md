Templates for generic programming are used to create data structures that can store any copyable data type while preserving the same interface and complexity guarantees. This allows for code reuse and flexibility in the types of data that can be stored in the data structures.

Singly + Doubly Linked List: The linked list implementation is designed to be memory efficient by defaulting to a singly linked structure, while also providing the option to use a doubly linked structure at compile time. This allows for flexibility in the choice of linked list implementation based on the specific needs of the application.

# 2

malloc calloc

make use of simple language 

shrink to fit if he knows no more new els